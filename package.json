{
  "name": "solar-system",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-assets.mjs",
    "preview": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
